---

- name: PostgreSQL | max fs files
  sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    sysctl_set: true
    state: present
    reload: true
  with_dict: "{{ pg_os_tuning_parameters }}"
  tags: postgresql,postgresql-os-config
