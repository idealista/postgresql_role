# Molecule managed

{% if item.registry is defined %}
FROM {{ item.registry.url }}/{{ item.image }}
{% else %}
FROM {{ item.image }}
{% endif %}


RUN apt-get clean && apt-get update -y && apt-get install -y python3 sudo bash ca-certificates gnupg systemd systemd-sysv && apt-get clean; 
